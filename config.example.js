/**
 * GitHub API Configuration Template
 *
 * This file serves as a template for your configuration.
 * Copy this file to config.js and fill in your values.
 *
 * 1. Copy this file: cp config.example.js config.js
 * 2. Edit config.js with your GitHub token (optional but recommended)
 *
 * Get a GitHub token at: https://github.com/settings/tokens
 *
 * Recommended scopes for public repositories:
 *   - public_repo (access to public repositories)
 *
 * The token provides 5000 requests/hour vs 60 unauthenticated.
 * Without a token, the site will still work but with stricter rate limits.
 */

const GITHUB_TOKEN = ''; // Your personal access token (optional but recommended)
const GITHUB_USERNAME = 'guillesrl'; // Your GitHub username
